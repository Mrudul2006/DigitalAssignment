//and gate
module and_usingmux (
input A,B,
  output Y
);
 assign Y = (~A &1)|(A & B);
endmodule

//or gate
module or_usingmux (
input A,B,
  output Y
);
 assign Y = (~A & B)|(A &1);
endmodule

//not gate
module not_usingmux (
input A,
 output Y
);
assign Y = (~A & 1)|(A & 1);
endmodule

